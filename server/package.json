{
  "name": "tech_assessment",
  "version": "1.0.0",
  "description": "The task is to create a UI (whether it a backend or frontend rendered page) which has an input box and a search button.",
  "author": "Faragó Dávid",
  "license": "UNLICENSED",
  "scripts": {
    "build": "tsc -p tsconfig.json || true",
    "build:watch": "tsc -w -p tsconfig.json || true",
    "start": "node dist/server.js",
    "test": "dotenv -e .env.test -- jest -i",
    "test:watch": "dotenv -e .env.test -- jest --watch"
  },
  "dependencies": {
    "@fastify/jwt": "8.0.0",
    "@fastify/cors": "8.5.0",
    "dotenv": "16.4.2",
    "fastify": "4.26.1",
    "uuid": "9.0.1",
    "zod": "3.22.4",
    "redis": "4.6.13"
  },
  "devDependencies": {
    "@tsconfig/node20": "20.1.2",
    "@types/jest": "^29.5.12",
    "@types/node": "20.11.17",
    "dotenv-cli": "7.3.0",
    "jest": "29.7.0",
    "ts-jest": "29.1.2",
    "typescript": "5.3.3"
  }
}